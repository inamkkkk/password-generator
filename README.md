# Password Generator

A Flutter app that generates strong, random passwords.

## Features

*   Customize password length
*   Choose character sets (numbers, symbols, uppercase letters)

## Getting Started

1.  Clone the repository
2.  Run `flutter pub get`
3.  Run `flutter run`
